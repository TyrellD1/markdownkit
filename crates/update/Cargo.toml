[package]
name = "markdownkit-update"
version = "0.3.0"
description = "GitHub release check and CLI self-update"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2.12", default-features = true }
