{
  "name": "markdownkit",
  "version": "0.1.0",
  "private": true,
  "description": "A beautiful, hardware-efficient macOS markdown viewer",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "cargo test --workspace",
    "icon": "tauri icon docs/markdownkit-icon.png -o src-tauri/icons",
    "install-app": "bash scripts/install.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
